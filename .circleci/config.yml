version: 2.1
jobs:
  build:
   docker:
      - image: ghcr.io/sushrut1101/docker:arch
   environment:
      GIT_EMAIL: 'dhruvtadvi777@gmail.com'
      GIT_NAME: 'dhruv'
      VERSION: '3.0.0'
      PBRP_MANIFEST: 'android-10.0'
      VENDOR: 'tecno'
      DEVICE: 'KE6'
   steps:
      - run:
          name: "TWRP Builder"
          command: |
            useradd -G wheel,audio,video -m natu -p natu
            dhruv | passwd natu
            dhruv | su natu
            curl -sL https://github.com/sarthakroy2002/Scripts/raw/main/Installation%20Scripts/arch_envsetup.sh | bash
           
workflows:
  version: 2.1
  remote_builder:
    jobs:
      - build:
          context: org-global
